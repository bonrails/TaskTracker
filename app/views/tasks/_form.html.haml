= form_for @task do |f|
  - if @task.errors.any?
    #error_explanation
      %h2= "#{pluralize(@task.errors.count, "error")} prohibited this task from being saved:"
      %ul
        - @task.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  -#.field
  -#  = f.label :user_id
  -#  = f.select :user_id, @users.collect {|x| [x.name, x.id]}
  .actions
    = f.submit 'Save'
